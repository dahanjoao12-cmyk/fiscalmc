"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export function NewCompanyForm(){const router=useRouter(),[form,setForm]=useState({legalName:"",taxId:"",municipalityCode:""}),[error,setError]=useState(""),[saving,setSaving]=useState(false);async function save(e:React.FormEvent){e.preventDefault();setSaving(true);const r=await fetch('/api/admin/organizations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await r.json();setSaving(false);if(!r.ok){setError(data.error??'Não foi possível criar a empresa.');return;}router.push(`/admin/empresas/${data.organization.id}`);}return <form className="form-card" onSubmit={save}><div className="field"><label>Razão social<input required value={form.legalName} onChange={e=>setForm({...form,legalName:e.target.value})}/></label></div><div className="field"><label>CNPJ<input required inputMode="numeric" value={form.taxId} onChange={e=>setForm({...form,taxId:e.target.value})}/></label></div><div className="field"><label>Código IBGE do município<input required inputMode="numeric" value={form.municipalityCode} onChange={e=>setForm({...form,municipalityCode:e.target.value})}/></label></div>{error&&<p className="alert error">{error}</p>}<div className="form-actions"><button className="button primary" disabled={saving}>{saving?'Salvando…':'Criar empresa bloqueada'}</button></div></form>}
