# Assinatura XML

A assinatura XML permanece atrás do `NationalNFSeProvider` bloqueado. A implementação
deve ser feita contra o XSD e as regras oficiais vigentes, com testes de canonicalização,
referência/ID, digest e verificação criptográfica antes de habilitar o provider nacional.
