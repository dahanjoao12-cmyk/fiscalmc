import "server-only";
import { createAdminClient } from "@/lib/supabase/admin";
import { SafeFiscalError } from "./errors";
import { MunicipalParametersProvider, issPercentToBasisPoints, parseMunicipalServiceCode } from "./municipal-parameters/client";
export type FiscalResolutionInput={municipalityCode:string;nationalTaxCode:string;municipalServiceCode?:string|null;taxRegime:"SIMPLES_NACIONAL"|"LUCRO_PRESUMIDO"|"LUCRO_REAL";reviewedAt?:string|null;serviceDate:string};
export type FiscalResolution={municipalityCode:string;nationalTaxCode:string;municipalServiceCode:string;iss:{rateBasisPoints:number;withheld:boolean;source:"MUNICIPAL_INTEGRATION"};retention:Record<string,never>;source:"MUNICIPAL_PARAMETERS";validity:{validFrom:string;validUntil?:string}};
export async function resolveFiscalConfiguration(input:FiscalResolutionInput):Promise<FiscalResolution>{if(!input.reviewedAt)throw new SafeFiscalError("FISCAL_CONFIGURATION_INCOMPLETE","Uma configuração fiscal desta empresa precisa ser revisada pelo escritório antes da emissão.");if(!input.municipalServiceCode)throw new SafeFiscalError("FISCAL_SERVICE_MAPPING_MISSING","Uma configuração fiscal desta empresa precisa ser revisada pelo escritório antes da emissão.");const serviceCode=parseMunicipalServiceCode(input.municipalServiceCode);const admin=createAdminClient();const{data:cached}=await admin.from("municipal_tax_rules").select("incidence,iss_rate_percent,valid_from,valid_until").eq("municipality_code",input.municipalityCode).eq("service_code",serviceCode).lte("valid_from",`${input.serviceDate}T23:59:59Z`).or(`valid_until.is.null,valid_until.gte.${input.serviceDate}T00:00:00Z`).order("valid_from",{ascending:false}).limit(1).maybeSingle();const rule=cached??await fetchRule(input.municipalityCode,serviceCode,input.serviceDate);if(rule.incidence!=="SIM"||rule.iss_rate_percent===null)throw new SafeFiscalError("FISCAL_CONFIGURATION_INCOMPLETE","Uma configuração fiscal desta empresa precisa ser revisada pelo escritório antes da emissão.");return{municipalityCode:input.municipalityCode,nationalTaxCode:input.nationalTaxCode,municipalServiceCode:serviceCode,iss:{rateBasisPoints:issPercentToBasisPoints(Number(rule.iss_rate_percent)),withheld:false,source:"MUNICIPAL_INTEGRATION"},retention:{},source:"MUNICIPAL_PARAMETERS",validity:{validFrom:rule.valid_from,...(rule.valid_until?{validUntil:rule.valid_until}:{})}};}
async function fetchRule(municipalityCode:string,serviceCode:string,competence:string){const response=await new MunicipalParametersProvider().getAliquota({municipalityCode,serviceCode,competence});const item=response.aliquotas[serviceCode].find(value=>!value.DtFim||value.DtFim>=`${competence}T00:00:00`)??response.aliquotas[serviceCode][0];const row={municipality_code:municipalityCode,service_code:serviceCode,incidence:item.Incidencia,iss_rate_percent:item.Aliq,valid_from:item.DtIni,valid_until:item.DtFim,source:"ADN_PRODUCTION_RESTRICTED",source_version:null,fetched_at:new Date().toISOString()};const{error}=await createAdminClient().from("municipal_tax_rules").upsert(row,{onConflict:"municipality_code,service_code,incidence,valid_from,source"});if(error)throw new SafeFiscalError("MUNICIPAL_PARAMETERS_INVALID","Não foi possível registrar os parâmetros municipais.");return{incidence:row.incidence,iss_rate_percent:row.iss_rate_percent,valid_from:row.valid_from,valid_until:row.valid_until};}
