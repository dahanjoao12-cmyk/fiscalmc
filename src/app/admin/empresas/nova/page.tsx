import { redirect } from "next/navigation";import { AppShell } from "@/components/app-shell";import { NewCompanyForm } from "@/components/new-company-form";import { requireOfficeSession } from "@/lib/auth/session";
export default async function NewCompanyPage(){try{await requireOfficeSession();}catch{redirect('/app?notice=office');}return <AppShell active="companies" admin><div className="page"><div className="page-heading"><div><h1>Nova empresa</h1><p>Cadastre os dados mínimos. A emissão seguirá bloqueada até revisão fiscal.</p></div></div><NewCompanyForm/></div></AppShell>}
