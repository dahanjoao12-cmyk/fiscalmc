"use client";
import { useState } from "react";
import { AppShell } from "@/components/app-shell";
import { MockBanner } from "@/components/mock-banner";
const steps=["Empresa","Configuração fiscal","Serviços","Certificado A1","Teste","Usuários","Ativação"];
export default function NewCompanyPage(){const[step,setStep]=useState(1);return <AppShell active="companies" admin><div className="page"><MockBanner/><div className="page-heading"><div><h1>Nova empresa</h1><p>Etapa {step} de {steps.length} — {steps[step-1]}</p></div></div><div className="form-card">{step===1&&<><h2>Dados cadastrais</h2><div className="field"><label htmlFor="legalName">Razão social</label><input className="input" id="legalName"/></div><div className="field"><label htmlFor="taxId">CNPJ</label><input className="input" id="taxId" autoCapitalize="characters"/></div><div className="field"><label htmlFor="municipality">Município</label><input className="input" id="municipality"/></div></>}{step===2&&<><h2>Configuração fiscal</h2><p style={{color:"var(--muted)"}}>Somente o escritório pode alterar estes dados.</p><div className="field"><label htmlFor="regime">Regime tributário</label><select className="input" id="regime"><option>Simples Nacional</option><option>Lucro Presumido</option><option>Lucro Real</option></select></div><div className="field"><label htmlFor="im">Inscrição municipal</label><input className="input" id="im"/></div></>}{step===3&&<><h2>Serviços</h2><p>Cadastre ao menos um modelo com as regras fiscais revisadas.</p><button className="button secondary">Adicionar serviço</button></>}{step===4&&<><h2>Certificado A1</h2><div className="alert error">O arquivo e a senha são processados somente no servidor, criptografados e nunca poderão ser baixados.</div><div className="field"><label htmlFor="cert">Arquivo PFX ou P12</label><input className="input" id="cert" type="file" accept=".pfx,.p12"/></div><div className="field"><label htmlFor="certPassword">Senha do certificado</label><input className="input" id="certPassword" type="password" autoComplete="new-password"/></div></>}{step===5&&<><h2>Teste de integração</h2><p>Validaremos configuração, certificado e acesso ao ambiente restrito sem emitir em produção.</p><button className="button secondary">Executar teste seguro</button></>}{step===6&&<><h2>Convidar usuários</h2><div className="field"><label htmlFor="invite">E-mail do cliente</label><input className="input" id="invite" type="email"/></div></>}{step===7&&<><h2>Ativação</h2><p>A aptidão é calculada pelo servidor. Não é um checkbox manual.</p><div className="alert success">Quando cadastro, fiscal, serviço, certificado e teste estiverem válidos, a empresa será liberada para emissão em homologação.</div></>}<div className="form-actions"><button className="button secondary" disabled={step===1} onClick={()=>setStep(step-1)}>Voltar</button><button className="button primary" onClick={()=>step<steps.length&&setStep(step+1)}>{step===steps.length?"Concluir cadastro":"Continuar"}</button></div></div></div></AppShell>}
