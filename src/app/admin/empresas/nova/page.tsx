import { redirect } from "next/navigation";import { NewCompanyForm } from "@/components/new-company-form";import { requireOfficeSession } from "@/lib/auth/session";
export default async function NewCompanyPage(){try{await requireOfficeSession();}catch{redirect('/app?notice=office');}return <div className="page"><div className="page-heading company-page-heading"><div><p className="eyebrow">Cadastro inicial</p><h1>Nova empresa</h1><p>Registre os dados cadastrais agora. A configuração fiscal será feita depois, com revisão do escritório.</p></div></div><NewCompanyForm/></div>}
