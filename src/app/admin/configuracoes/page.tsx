import { redirect } from "next/navigation";import { requireOfficeSession } from "@/lib/auth/session";
export default async function Settings(){try{await requireOfficeSession();}catch{redirect('/app?notice=office');}return <div className="page"><div className="page-heading"><div><h1>Configurações</h1><p>Ambiente, integrações e segurança.</p></div></div><section className="section"><h2 className="section-title">Ambiente fiscal</h2><div className="row" style={{gridTemplateColumns:"1fr auto"}}><div><strong>Produção restrita</strong><p style={{margin:"5px 0",color:"var(--muted)"}}>Definido por NFSE_ENV no deploy. Produção permanece bloqueada.</p></div><span className="status warning">Homologação</span></div></section></div>}
