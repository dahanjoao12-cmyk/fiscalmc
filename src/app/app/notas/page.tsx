import { InvoiceList,type InvoiceRow } from "@/components/invoice-list";import { requireClientPageSession } from "@/lib/auth/session";import { createAdminClient } from "@/lib/supabase/admin";
export default async function InvoicesPage(){const session=await requireClientPageSession();const{data}=await createAdminClient().from('invoices').select('id,dps_number,amount_cents,status,service_date,customers(legal_name),service_templates(name)').eq('organization_id',session.organizationId).order('created_at',{ascending:false}).limit(100);const rows:InvoiceRow[]=(data??[]).map(x=>({id:x.id,number:x.dps_number?.toString()??'—',amountCents:x.amount_cents,status:x.status,date:x.service_date,customer:x.customers?.[0]?.legal_name??'Tomador não disponível',service:x.service_templates?.[0]?.name??'Serviço não disponível'}));return <div className="page"><div className="page-heading"><div><h1>Notas fiscais</h1><p>Consulte as emissões da sua empresa.</p></div></div><InvoiceList all rows={rows}/></div>}
