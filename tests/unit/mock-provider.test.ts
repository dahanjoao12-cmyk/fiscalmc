import { describe,expect,it } from "vitest"; import { MockNFSeProvider } from "@/lib/nfse/issuance/mock-provider"; import { buildFiscalDocument } from "@/lib/nfse/issuance/domain";
const document=buildFiscalDocument({organizationId:"org",amountCents:150000,serviceDate:"2026-08-24",description:"Consultoria",dpsNumber:1n});
describe("MockNFSeProvider",()=>{it("emite e mantém idempotência",async()=>{const provider=new MockNFSeProvider();const a=await provider.issue({document,idempotencyKey:"same"});const b=await provider.issue({document,idempotencyKey:"same"});expect(a).toEqual(b);expect(a.status).toBe("ISSUED")});it("não converte timeout em rejeição",async()=>{const result=await new MockNFSeProvider().issue({document,idempotencyKey:"timeout",scenario:"timeout"});expect(result.status).toBe("UNKNOWN")});it("sanitiza rejeição para cliente",async()=>{const result=await new MockNFSeProvider().issue({document,idempotencyKey:"reject",scenario:"rejection"});expect(result.status).toBe("REJECTED");if(result.status==="REJECTED")expect(result.safeMessage).not.toContain("mismatch")});});
